// Helper functions for the image pipeline.

import { ImageData } from "../types";

// Function to run a Python script and process image data.
export async function runPythonScript(
  scriptPath: string,
  image: ImageData,
): Promise<ImageData> {
  // ...implementation to call Python script using child_process.spawn
  scriptPath;
  return image;
}

// ...other helper functions as needed.
