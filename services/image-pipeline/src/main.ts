// main.ts
// Main entry point for the image-pipeline service for manual testing
// during development.
void (async function MAIN() {
  console.log(`at: MAIN from ${__filename}`);
  // Lests start development here and we will split later in their own
  // files and modules.

  // 1. Load the configuration including the path to the folder where
  // the images are stored.

  // 2. Load the images from the folder.

  // 3. Process the images.

  // 4. Save the processed images metadata to a database.

  // 5. Save the processed images to a folder.


  return void 0;
})();

// Interfaces goes here


// Types goes here
